<UserControl x:Class="Практическая7.View.SynergyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Практическая7.View"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="800">
    <Grid Background="DarkSalmon">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50" />
            <ColumnDefinition />
        </Grid.ColumnDefinitions>

        <Image Grid.Column="0" Width="30" Height="30" Source="{Binding Icon}" />
        <StackPanel Grid.Column="1" VerticalAlignment="Center" Margin="5,0,0,0">
            <Label Content="{Binding Label}" />
            <Label Content="{Binding Effect}" />
        </StackPanel>
    </Grid>
</UserControl>
